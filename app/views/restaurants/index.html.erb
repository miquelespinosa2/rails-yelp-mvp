<div class="container">
  <ul>
    <% @restaurants.each do |restaurant| %>
      <li><%= restaurant.name %> - <%= link_to '👀', restaurant_path(restaurant) %> - <%= link_to '✏️', edit_restaurant_path(restaurant) %> - <%= link_to '🗑️', restaurant, method: :delete, data: { confirm: 'Are you sure?' } %></li>

      <% end %>
  </ul>
</div>

<%= link_to 'New Restaurant', new_restaurant_path %>
